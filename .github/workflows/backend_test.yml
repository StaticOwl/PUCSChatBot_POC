name: Backend Test
run-name: ${{ github.actor }} is testing backend working or not 🚀
on: [push]

jobs:
  build:
    name: FrontEnd Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: Go to Directory
        run: cd ./frontend/pfw_chatbot/
      - name: list files
        run: ls
      - name: Install dependencies
        run: npm install
      - name: Test
        run: npm test